
  // =============================================================================
  // Lazy Load
  // =============================================================================

  barberry.lazyLoad = function() {

	$("img.lazy").lazyload({
    	threshold : 200
	});

  }
